# 当前进度

## 本文件用途

这份文件不是历史流水账，只负责回答 5 件事：

1. 当前在哪个板块
2. 当前板块是否已经清楚
3. 当前还没收口的核心问题
4. 进入下一阶段前必须满足什么条件
5. 最近最需要记住的坑点是什么

每轮任务结束后默认重写，不持续往后追加。

---

## 当前所在板块

当前处在：

**第三工作台结构与问题复盘库模型收口阶段**

不是：

- 第三工作台实现层已经完成
- 可以进入联调验收

---

## 当前板块是否清楚

当前状态：

**主方向已经清楚，但 UI 结构、问题闭环表达、数据分层文档刚刚冻结，仍在收口中。**

---

## 已确认的核心结论

- 真实代码库以 `D:\codex-program\codex-sale-data\客户线索库\.obsidian\plugins\sales-workbench-prototype` 为准
- 第三工作台不再按客户异常整改台理解，而是经营复盘与策略纠偏台
- 页面最终结构固定为 `左选 / 中操 / 右辅`
- 客户数据与问题复盘库必须分开存放，不能混成一个库
- 右栏不是普通证据区，而是问题闭环辅助区
- 右栏固定顺序为：发现问题 -> 原因分析 -> 样本客户链接 -> 建议调整 -> 待验证 -> 已尝试动作折叠区 -> AI skill 按钮
- 状态门必须存在，并明确支持 `已改判`

---

## 当前未收口的点

- 三栏结构还没有在真实第三工作台 UI 上稳定落地
- 中栏市场漏斗表达还需要在“完整展示”和“别太大”之间拿到刚好的尺度
- 中栏销售阶段还需要改成标准矩阵表格
- 右栏问题闭环顺序和状态门还没有正式做成界面结构
- AI 标记和 skill 选择按钮还没有正式预留到位

---

## 最近必须记住的坑点

- 不能再拿错误代码目录做判断
- 不能把早期原型或 Claude 汇报直接当成真实完成
- 不能把“好看”误当成“适合复盘工作台”
- 不能把客户事实层和问题经验层混成一个数据对象
- 不能跳过“先发现问题”直接让 AI 先给建议
- 不能把 `已改判` 简单归到 `未解决`

---

## 进入下一阶段前的条件

只有满足下面条件，才建议进入更后面的实现或联调：

1. 第三工作台三栏结构已经稳定
2. 客户数据 / 问题复盘库 / 关联规则 / 状态门流转 已经在文档和界面口径上统一
3. 右栏问题闭环顺序已经固定
4. 中栏市场与销售视图都已经达到专业工作台表达
5. 所有 mock / AI 占位边界都写清楚

---

## 当前允许执行

当前允许继续做的事：

- 重写 PRD 和任务清单
- 调整第三工作台 UI 结构
- 收口字段、状态门、AI 接入口占位
- 审阅 Claude 的 UI 执行结果

当前不允许做的事：

- 把第三工作台说成真实闭环已完成
- 直接进入联调验收
- 直接宣称问题库数据库已落地
- 跳过文档收口去扩第一、第二工作台
